# API Tests

Keep all API tests under this directory.

- `unit/` is for pure logic and schema-level tests.
- `integration/` is for in-process HTTP and runtime contract tests.

Do not place tests under `crates/api/src`.
